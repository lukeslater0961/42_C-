#include "sed.hpp"

int	main(int argc, char **argv)
{
	if (argc != 4)
		return (error_mngment(ARG_ERROR));
	else
	{
		if (parseArgs(argv))
			return (1);
		std::ifstream* inFile = CheckAndOpen(argv);
		if (inFile)
			do_find_n_replace(inFile, argv[1]);
	}
}

